# author-tics.txt — 알려진 작성자 반복 구절 시드 목록
# author_repeat.py scan --seed 에 넘기면 프로필 없이도 이 표현들을 잡아낸다.
# 형식: 표현 => 대체 지시문  (=> 없으면 "평이한 말로 바꾼다" 기본 처리)
# ## 줄은 섹션 이름 (scan 출력의 "종류" 열에 "시드:섹션명"으로 표시)

## 에세이·분석문 (2026-08-22 stop-slop 분석에서 확인)
갈랐다 => 차이가 났다/결과가 달랐다
따로 논다 => 별개다/비례하지 않는다
뿌리가 된 => 바탕이 된/먼저 나온
거칠다 => 다듬어지지 않았다/정확하지 않다
못박았다 => 명시했다/정했다
못박아뒀다 => 명시해 뒀다/적어 뒀다
손은 내가 댔다 => 내가 직접 했다
하는 자리다 => (구체 동사로)
한눈에 => (볼드 단독 줄이면 그 줄을 지우고 본문을 바로 시작)
모양새 => 모습/상태
셈이다 => (빼고 직접 서술)
그 지점 => 그 부분/거기
들어맞는다 => 맞다/해당한다/확인했다

## 기술 게시글 (2026-08-22 study-hugo 169편 전수조사)
한눈에 => (볼드 단독 줄이면 그 줄을 지우고 본문을 바로 시작)
다만 => 문장을 나누거나 "그런데"/생략
동시에 => 두 문장으로
핵심은 => 프레이밍 구를 지우고 내용만
이것이 => 프레이밍 구를 지우고 내용만
정리하면 => 프레이밍 구를 지우고 내용만
구조적 => 구체 원인을 쓴다
반면 => 대조·반전 틀을 빼고 직접 서술
실은 => 대조·반전 틀을 빼고 직접 서술
두 가지 => 개수 예고 없이 바로 나열
세 가지 => 개수 예고 없이 바로 나열
얹어 => 추가하면/두면
얹으면 => 추가하면/두면
얹는 => 추가하는/두는
얹고 => 추가하고/두고
정확히 => 대부분 지워도 뜻이 같다. 빼거나 구체 수치로 바꾼다
조용히 => 실제 동작(로그 없이/알림 없이)으로 쓴다
질문을 던지 => 묻다/질문하다
물음을 던지 => 묻다/질문하다

# 보류: 여기서/근본적으로/맥락을 (매치 안 됨)

## 대화 답변 (2026-08-25 세션 발화 4.8만자 자기측정)
고리를 => (비유. 실제 동작·순서로 쓴다)
고리가 => (비유. 실제 동작·순서로 쓴다)
바퀴째 => (비유. "반복했다/다시 돌렸다"로)
바퀴 돌 => (비유. "반복했다/다시 돌렸다"로)
정리하겠습니다
정리합니다
한 가지
몫 => (비유. 담당·판단 주체를 명시)
# 보류(대화 기능어 — 차단하면 답변이 부자연): 맞습니다(14) 확인합니다(8) 말씀해 주세요(7) 잡습니다(7) 확인했습니다(5) 진행합니다(4)

## 대화 답변 2차 (2026-08-30 사용자 지적 8종 — 부분열·활용형 적대 검증 후 확정)
가리키 => 뜻한다/말한다 (포인터·심링크·참조 실지시는 예외)
가리킨다 => 뜻한다/말한다 (실지시는 예외)
가리킵니다 => 뜻합니다/말합니다 (실지시는 예외)
가리켰다 => 뜻했다/말했다 (실지시는 예외)
가리킬 => 뜻할/말할 (실지시는 예외)
엇갈 => 다르다/불일치한다
그려지 => (비유 피동. 된다/예상된다로 직접 서술. 실제 다이어그램·그래프 렌더링 서술은 예외)
그려진다 => (비유 피동. 실제 렌더링 서술은 예외)
그려집니다 => (비유 피동. 실제 렌더링 서술은 예외)
그려졌다 => (비유 피동. 실제 렌더링 서술은 예외)
그려져 => (비유 피동. 실제 렌더링 서술은 예외)
그려질 => (비유 피동. 실제 렌더링 서술은 예외)
것같 => (추측 헤지. 확실하면 단정, 불확실하면 근거를 쓴다)
것 같 => (추측 헤지. 확실하면 단정, 불확실하면 근거를 쓴다)
거같 => (추측 헤지. 확실하면 단정, 불확실하면 근거를 쓴다)
거 같 => (추측 헤지. 확실하면 단정, 불확실하면 근거를 쓴다)
다음과 같습니다 => 프레이밍 구를 지우고 내용만
아래와 같습니다 => 프레이밍 구를 지우고 내용만
느냐입니다 => (의문형 명사절 종결. 평서문으로 바꾼다)
남깁니다 => (습관성 클로징이면 삭제. 실제 기록 행위 서술이면 유지)
남기겠습니다 => (습관성 클로징이면 삭제. 실제 기록 행위 서술이면 유지)
남겼습니다 => (습관성 클로징이면 삭제. 실제 기록 행위 서술이면 유지)
맞바꾸 => (비유. 얻는 것과 잃는 것을 구체적으로 쓴다)
맞바꾼다 => (비유. 얻는 것과 잃는 것을 구체적으로 쓴다)
맞바꿉니다 => (비유. 얻는 것과 잃는 것을 구체적으로 쓴다)
맞바꿨다 => (비유. 얻는 것과 잃는 것을 구체적으로 쓴다)
맞바꿔 => (비유. 얻는 것과 잃는 것을 구체적으로 쓴다)
맞바꿀 => (비유. 얻는 것과 잃는 것을 구체적으로 쓴다)
맞바꿈 => (비유. 얻는 것과 잃는 것을 구체적으로 쓴다)
투기합니다 => (비유. 실제 투자·투기 문맥이면 유지, 습관어면 '근거 없이 판단하지 않는다'로)
# 주의: '투기합니다'는 '격투기합니다' 부분열 오탐 가능(구조상 배제 불가) — 실사용 오탐 관찰 시 폐기 검토
# 오탐 방지로 좁힘: 냐입니다(케냐입니다 등 고유명사 충돌)→느냐입니다, 와/과 같습니다('1절과 같습니다' 등 정상 비교문 다수)→다음과/아래와 결합형만
