# EVENT: settings.json의 hooks.<EVENT> 배열에 등록된다.
# MATCHER: 있으면 등록 항목에 "matcher" 키로 들어간다 (PreToolUse류만 의미 있음).
# ENTRY: 이 디렉토리 안의 진입점 파일명 — 심링크 목적지 파일명도 동일하게 쓴다.
EVENT=PreToolUse
MATCHER=^Workflow$
ENTRY=workflow-arm.py
